const RideStates = Object.freeze({
    PROPOSED: 'proposed',
    REJECTED: 'rejected',
    ACCEPTED: 'accepted',
    COMPLETED: 'completed',
    CANCELLED: 'cancelled'
  });
  
  module.exports = RideStates;